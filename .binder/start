#!/usr/bin/env bash
set -euo pipefail
REPO_ROOT="${REPO_DIR:-$PWD}"
cd "$REPO_ROOT"
exec jupyter lab \
  --ip=0.0.0.0 \
  --port=8888 \
  --no-browser \
  --NotebookApp.default_url=/lab \
  --ServerApp.allow_remote_access=True
